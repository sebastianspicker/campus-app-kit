# Deep Audit Report — 2026-04-26

## Objective

Perform iterative repository-wide audits and remediation passes across architecture, scripts, runtime logic, tests, and developer workflows.

## System map (how components work together)

- **Mobile app (`apps/mobile`)**: Expo Router UI for Today/Events/Rooms/Schedule/Profile; hooks fetch BFF resources and render with shared schemas.
- **BFF (`apps/bff`)**: Node HTTP server with middleware chain (CORS → security headers → optional auth → rate limit → method guard) and normalized public-data routes.
- **Shared package (`packages/shared`)**: cross-package domain and error contracts.
- **Institution package (`packages/institutions`)**: institution-specific public pack data used by BFF route/connectors.
- **Monorepo runtime**: turbo runs package build/lint/typecheck/test tasks with shared workspace tooling.

## Iterative audit log (20 passes)

| Pass | Focus | Outcome |
|---|---|---|
| 1 | Repo topology and architecture docs | Verified package boundaries and data flow. |
| 2 | Root scripts and automation | Identified setup-script robustness opportunity. |
| 3 | Mobile scripts | Identified wrapper duplication pattern. |
| 4 | BFF middleware path | No P0/P1 issue observed in request chain ordering. |
| 5 | BFF cache implementation | Identified minor hot-path optimization opportunity. |
| 6 | BFF rate limit utility | No blocking correctness issue observed. |
| 7 | Mobile hooks and API helpers | No blocking issue in resource-loading semantics observed. |
| 8 | Shared/institutions package contracts | No schema-level defect found in sampled paths. |
| 9 | Existing unit/integration tests | Coverage breadth confirmed across packages. |
| 10 | Build/lint/typecheck pipeline | Passed in current environment. |
| 11 | Script portability review | Confirmed shebang/strict-mode consistency gap in setup script. |
| 12 | Script argument safety | Added guard for empty helper-script invocation. |
| 13 | Cache TTL semantics | Identified and fixed avoidable cache write for non-positive TTL. |
| 14 | Cache timestamp calls | Reduced duplicate timestamp reads during cache write path. |
| 15 | BFF cache tests | Added regression test for zero/negative TTL no-cache behavior. |
| 16 | Mobile command wrapper behavior | Verified wrappers delegate correctly via shared launcher. |
| 17 | Developer setup path | Replaced global-install fallback with corepack-first pnpm install. |
| 18 | Full lint/typecheck/test rerun | Passed post-remediation. |
| 19 | Dependency advisory audit | Endpoint blocked by HTTP 403 (environmental). |
| 20 | Final consolidation review | No additional P0/P1/P2 issues found in this pass. |

## Issues found and remediated

### P0
- None found in this audit pass.

### P1
- None found requiring immediate production hotfix in this audit pass.

### P2 remediations applied

1. **Mobile script deduplication**
   - Centralized mobile command launch behavior in `apps/mobile/scripts/run-mobile-command.sh`.
   - Updated wrapper scripts to delegate to shared helper.

2. **Developer setup script hardening**
   - Standardized shebang and strict mode.
   - Added `corepack`-first pnpm installation fallback instead of global npm install.

3. **Cache behavior and micro-optimization**
   - Avoid cache writes when TTL is non-positive.
   - Use a single timestamp for `expiresAt` + `lastAccessedAt` during insert.

4. **Regression coverage**
   - Added test validating no-cache behavior when TTL is `0` or negative.

## Commands executed

- `pnpm lint`
- `pnpm typecheck`
- `pnpm test`
- `pnpm audit --prod` *(blocked by registry advisory endpoint HTTP 403 in this environment)*

## Final status

- No P0/P1 issues were identified in this 20-pass iterative review.
- All discovered P2 issues from this pass were remediated and validated by test/lint/typecheck.
