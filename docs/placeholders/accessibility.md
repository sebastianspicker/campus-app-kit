# Placeholder: Accessibility baseline (mobile UI)

**Why this is missing**
- The UI components (Cards, Sections, Screens) do not yet follow a defined accessibility baseline (labels, contrast, touch targets).

**TODO**
- Add `accessibilityRole` and labels (e.g. `accessibilityLabel`) to reusable UI components.
- Verify color contrast (text vs background).
- Ensure touch targets are at least 44px.
- Document how to verify accessibility in RN (manual checks + recommended tools).
