# Expo API routes

This repo runs the public API as a separate Node BFF (`apps/bff`).

If you want to deploy API routes via EAS Hosting, implement them in a separate branch or private repo and reuse:

- `@campus/institutions` for packs
- `@campus/shared` for schemas and error envelopes
