# Expo dev client

Use a dev client only when you need custom native code that Expo Go cannot load.

Commands:

- `pnpm --filter @campus/mobile dev`
- `pnpm --filter @campus/mobile build:preview`
- `pnpm --filter @campus/mobile build:production`
