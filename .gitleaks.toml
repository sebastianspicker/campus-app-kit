# Placeholder: gitleaks configuration
# TODO:
# - Add allowlist rules for known false positives (non-secret test data)
# - Add rules specific to this repo (e.g. disallow private endpoints)
# - Wire this config into `.github/workflows/gitleaks.yml` if desired

[allowlist]
description = "Campus App Kit allowlist (TODO)"
paths = [
  '''docs/''',
]

