# Copy to apps/client/.env for local client development.
# A physical device needs HTTPS or a locally trusted proxy instead of loopback.
EXPO_PUBLIC_BFF_BASE_URL=http://localhost:4000

# Preview and production builds also require INSTITUTION_ID.
# Production builds require institution-owned application identifiers.
# INSTITUTION_ID=hfmt
# MOBILE_BUNDLE_IDENTIFIER=edu.example.concourse
# MOBILE_ANDROID_PACKAGE=edu.example.concourse
