{
  // Placeholder: build tsconfig that actually emits JS
  // TODO:
  // - Keep `extends` aligned with `tsconfig.json`
  // - Ensure `outDir` matches Docker/production entrypoints
  // - Consider generating `.d.ts` for packages if exported
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": false,
    "declaration": false,
    "sourceMap": true
  }
}

