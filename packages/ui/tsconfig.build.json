{
  // Placeholder: build tsconfig for @campus/ui emit
  // TODO:
  // - Decide whether this package is needed; if yes, emit dist and add real components
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": false,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "outDir": "dist"
  },
  "include": ["src/**/*.ts", "src/**/*.tsx"]
}

