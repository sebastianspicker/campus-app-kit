{
  "name": "@campus/ui",
  "private": true,
  "version": "0.1.0",
  "types": "src/index.ts",
  "scripts": {
    "lint": "eslint src --ext .ts,.tsx",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  }
}
