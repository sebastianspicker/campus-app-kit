{
  // Placeholder: build tsconfig for @campus/shared emit
  // TODO:
  // - Emit to `dist/` and publish/consume via `exports` in package.json
  // - Ensure Node + Metro resolve the compiled entry correctly
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": false,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "outDir": "dist"
  },
  "include": ["src/**/*.ts"]
}

