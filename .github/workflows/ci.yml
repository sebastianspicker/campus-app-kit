name: verify
on:
  push:
    branches: [dev]
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: git status --short
add-e2e-dependency-revie: publication
github-actions: publication
split-lint-and-test-exec: publication
refresh-workflow-permiss: maintenance
